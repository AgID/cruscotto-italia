# Rate limiting Chat Lab: 20 richieste/minuto per IP
limit_req_zone $binary_remote_addr zone=chatlab:10m rate=20r/m;
# Limite connessioni simultanee per IP verso il chatbot (anti-abuso SSE)
limit_conn_zone $binary_remote_addr zone=chatconn:10m;
