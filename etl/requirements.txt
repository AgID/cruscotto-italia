# Core
duckdb>=1.0.0
pyarrow>=15.0.0
pandas>=2.2.0
requests>=2.32.0

# R2 / S3
boto3>=1.34.0
botocore>=1.34.0

# CLI / utility
click>=8.1.0
tqdm>=4.66.0
python-dateutil>=2.9.0

# Spreadsheet parsing (territorio.py + runts.py)
openpyxl>=3.1.0

# Shapefile + riproiezione coordinate (censimento.py)
# pyshp: legge shapefile sezioni di censimento ISTAT BT2021 (DBF + SHP).
# pyproj: riproietta da WGS84 UTM Zona 32N (EPSG:32632) a EPSG:4326
#   per servire GeoJSON direttamente al frontend Leaflet.
pyshp>=2.3.1
pyproj>=3.6.0

# Logging structured
structlog>=24.1.0

# Dev / test (install with: pip install -r requirements.txt -r requirements-dev.txt)
